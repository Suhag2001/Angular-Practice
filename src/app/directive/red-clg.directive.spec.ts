import { RedClgDirective } from './red-clg.directive';

describe('RedClgDirective', () => {
  it('should create an instance', () => {
    const directive = new RedClgDirective();
    expect(directive).toBeTruthy();
  });
});
